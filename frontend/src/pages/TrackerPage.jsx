/*
TrackerPage — the core feature page.
Will host the webcam view, (pose skeleton overlay?), rep counter, and form score.
Will be fully implemented after all hooks and components are ready.
*/
function TrackerPage() {
	return (
		<div>
			<h1>Tracker</h1>
			<p>Pose detection coming soon.</p>
		</div>
	);
}

export default TrackerPage;
